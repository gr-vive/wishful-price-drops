name: Daily reprice

on:
  schedule:
    - cron: "0 7 * * *"   # каждый день в 07:00 UTC
  workflow_dispatch:

jobs:
  call-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Call internal reprice
        run: |
          curl -X POST https://ТВОЙ_ДЕПЛОЙ_БЭКЕНДА/internal/reprice
